/// Represents a document that can be used to create a signature request.
final class SignatureDocumentOption {
  const SignatureDocumentOption({required this.id, required this.title});

  final String id;
  final String title;
}
